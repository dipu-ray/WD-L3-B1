## Date: 17 February, 2026 - Tuesday

## Topics:
- Recap HTML Tables and Forms
- HTML Favicon and HTML Iframes
- HTML JavaScript and Others
- CSS - Cascading Style Sheets

<hr>

### 1. Recap HTML Tables and Forms
- HTML Table Elements:
    - HTML Elements:
        - `<table>`
        - `<thead>`
        - `<tr>`
        - `<th>`
        - `<tbody>`
        - `<td>`
        - `<tfoot>`
    - CSS Properties:
        - `border-collapse:`
        - `width:`
        - `border:`
        - `padding:`
- HTML Forms Elements:
    - HTML Elements:
        - `<form>`
        - `<label>`
        - `<input>`
        - `<textarea>`
        - `<select>`
        - `<fieldset>`
        - `<legend>`
        - `<optgroup>`
        - `<option>`
        - `<progress>`
        - `<reset>` - Reset button in forms
        - Input Types Attributes:
            - `type="text"`
            - `type="email"`
            - `type="password"`
            - `type="number"`
            - `type="checkbox"`
            - `type="radio"`
            - `type="button"`
            - `type="submit"`
            - `type="image"`
            - `type="range"`
            - `type="date"`
            - `type="file"`
        - HTML Form Attributes:
            - `action=""`
            - `method=""`
            - `enctype=""`
            - `target=""`
            - `id=""`
            - `name=""`
            - `value=""`
            - `placeholder=""`
            - `rows=""`
            - `cols=""`
            - `required`
            - `disabled`
            - `multiple`
    - CSS Properties:
        - `width:`
        - `outline:`
        - `padding:`
        - `color:`
        - `background-color:`

<hr>

### 2. HTML Favicon and HTML Iframes
- HTML Favicon:
    - Peak a image and make it 16/16 for favicon.
    - Search `favicon generator` and generate image for favicon.
    - `realfavicongenerator.net` - show icon how to website for other devices.
    - There have 5 link to attached head to favicon for standard.
- HTML Iframes:
    - `<iframe>` - HTML iframe element.
    - `frameborder=""`
    - `width=""`
    - `height=""`
    - `uBlock origin lite` - for block elements in browser.

<hr>

### 3. HTML JavaScript and Others
- HTML JavaScript:
    - `<script>`
- And Others: - Just basic idea
    - `HTML File Path`
    - `HTML Head`
    - `HTML Layout`
    - `HTML Responsive`
    - `HTML Computercode`
    - `HTML Semantics`
    - `HTML Entities` - See all things
    - `HTML Symbols` - Almost are entities
    - `HTML Emojis`
    - `HTML Charsets`
    - `HTML URL Encode`
        - HTTPs - 443 port
        - HTTP - 80 port
    - `HTML vs XHTML`
- Visit `themewagon` web and see html and css template for idea or better understanding to web design.

<hr>

### 4. CSS - Cascading Style Sheets
- `CSS Home`
- `CSS Introduction`
- `tutorialrepublic.com` - Website for learn HTML and CSS
- `CSS Syntax`
- `CSS Selectors` - Universal, Element, Id and Class